{
  "name": "immutable-store",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "npx doctest cleanup && npx doctest build && npx jest && npx doctest cleanup"
  },
  "keywords": [
    "shared state",
    "store",
    "immutable"
  ],
  "author": "axtk",
  "license": "ISC",
  "dependencies": {
    "immutable": "^4.0.0-rc.12"
  },
  "devDependencies": {
    "@babel/plugin-transform-modules-commonjs": "^7.10.4",
    "doctest": "github:axtk/doctest",
    "jest": "^26.4.2"
  }
}
