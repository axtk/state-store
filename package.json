{
  "name": "@axtk/store",
  "version": "1.3.2",
  "description": "A lightweight storage for shared state",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --outfile=index.build.js --global-name=Store",
    "build:min": "npm run build -- --minify",
    "test": "node -r esm test.js"
  },
  "keywords": [
    "store",
    "shared state"
  ],
  "author": "axtk",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git://github.com/axtk/store.git"
  },
  "dependencies": {
    "@axtk/event-manager": "^1.1.3",
    "immutable": "^4.0.0-rc.12",
    "lodash": "^4.17.20"
  },
  "devDependencies": {
    "esbuild": "^0.8.52",
    "esm": "^3.2.25"
  },
  "publishConfig": {
    "access": "public"
  }
}
